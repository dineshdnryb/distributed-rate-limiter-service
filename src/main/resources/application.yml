server:
  port: 8080

spring:
  application:
    name: rate-limiter-service
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info
  
